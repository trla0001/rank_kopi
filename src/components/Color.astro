---
const { img, type } = Astro.props;
---

<div class={type}>
  <img src=`/img/webp/colors/${img}` />
</div>

<style>
  div {
    width: 19px;
  }
  img {
    border-radius: var(--radius_max);
  }
  .card {
    width: 17px;
  }

  .singleview {
    width: 32px;
  }
</style>
