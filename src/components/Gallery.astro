---
const { img, img_1, img_2, img_3 } = Astro.props;
---

<article>
  <img src=`/img/webp/products/${img}` alt="Produktbillede" class="main" />
  <div class="">
    <img src=`/img/webp/products/${img_1}` class="img_1" alt="et andet produktbillede" />
    <img src=`/img/webp/products/${img_2}` class="img_2" alt="et tredje produktbillede" />
  </div>
</article>

<style>
  article {
    display: grid;
    grid-template-columns: 3fr 0.95fr;
    gap: var(--spacing_4);
    padding: 0 var(--spacing_5);
  }

  div {
    display: flex;
    flex-direction: column;
    gap: var(--spacing_4);
    grid-column: 2 / span 1;
  }

  .main {
    grid-column: 1 / span 1;
  }

  .hide {
    display: none;
  }
  img {
    border-radius: var(--radius_m);
  }

  @media (min-width: 800px) {
    article {
      grid-template-columns: 0.95fr 3fr;
    }

    div {
      display: flex;
      flex-direction: column;
      gap: var(--spacing_4);
      grid-column: 1 / span 1;
      grid-row: 1 / span 1;
    }

    .main {
      grid-column: 2 / span 1;
    }
  }
</style>
